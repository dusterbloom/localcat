[pytest]
testpaths = server/tests
markers =
    unit: fast unit tests (no I/O or network)
    integration: component/integration tests
    e2e: end-to-end tests (optional/slow)
    asyncio: asynchronous test using event loop
addopts = -q
