# Core dependencies (pinned for stability)
python-dotenv==1.0.1
fastapi[all]==0.115.7
uvicorn==0.35.0  # websockets warning is from uvicorn internals, not fixable
openai>=1.74.0,<=1.99.1  # Compatible with pipecat-ai requirements

# Computer vision and ML
opencv-python==4.10.0.84
mlx-lm==0.20.2
mlx-audio==0.2.5

# Pipecat with specific extras
pipecat-ai[openai,deepgram,rime,silero,mlx-whisper,local-smart-turn]>=0.0.80

# WebRTC
aiortc==1.10.1

# NLP
nltk==3.9.1

# HotMem ultra-fast memory system
lmdb==1.5.1
msgpack==1.0.8
rapidfuzz==3.9.6
pycld3==0.22  # Language detection
spacy==3.7.5  # UD parsing (download models separately)
sentence-transformers==2.7.0  # Optional: cross-lingual relation normalization
onnxruntime==1.18.1  # ONNX local inference (unified wheels incl. macOS arm64)
transformers==4.43.3  # Tokenizers + fast offset mapping for ONNX taggers
Unidecode==1.3.8  # Transliteration for cross-lingual aliasing
fastcoref==3.2.0  # Optional neural coreference (local, HF models)

# ML dependencies (pinned for compatibility)
scikit-learn==1.5.1  # Downgraded for coremltools compatibility
torch==2.5.0  # Downgraded to tested version with coremltools
coremltools==8.3.0

# Note: vllm removed (not compatible with macOS)
