# Core dependencies (pinned for stability)
python-dotenv==1.0.1
fastapi[all]==0.115.7
uvicorn==0.35.0  # websockets warning is from uvicorn internals, not fixable
httpx==0.28.1  # HTTP client for FastAPI streaming TTS
openai>=1.74.0,<=1.99.1  # Compatible with pipecat-ai requirements

# Computer vision and ML
opencv-python==4.10.0.84
mlx-lm==0.28.3  # REQUIRED: Updated for macOS Sequoia compatibility
mlx-audio==0.2.5
mlx==0.29.3  # REQUIRED: Updated for macOS Sequoia compatibility
mlx-metal==0.29.3  # REQUIRED: Matches mlx version

# Pipecat with specific extras
pipecat-ai[openai,deepgram,rime,silero,mlx-whisper,local-smart-turn]>=0.0.80

# WebRTC
aiortc==1.10.1

# NLP
nltk==3.9.1

# HotMem ultra-fast memory system
lmdb==1.5.1
msgpack==1.0.8
rapidfuzz==3.9.6
spacy>=3.8.4  # UD parsing (download models separately) - updated for mlx-audio compatibility
tiktoken>=0.7.0  # Accurate token counting for context management


# Audio Intelligence (Session 1: Speaker Recognition)
torch==2.9.0  # REQUIRED: 2.9.0+ for macOS Sequoia compatibility (fixes ML process killing bug)
torchaudio==2.9.0  # REQUIRED: Matches torch version
speechbrain>=1.0.0  # Unified speaker recognition, emotion, prosody (requires manual patch for torchaudio 2.9.0)
praat-parselmouth>=0.4.3  # Prosody analysis (Session 3)